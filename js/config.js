const CONFIG = {
  // Movement detection sensitivity for tracked red points.
  movementThreshold: 4.5,
  matchDistance: 30,
  minRedArea: 8,

  // Radius around moving points where glitch fragments appear.
  glitchRadius: 150,

  // Number of floating rectangles in the HSB background.
  rectCount: 600,

  // Text mask animation intensity and temporal speed.
  wobbleAmount: 2.5,
  wobbleSpeed: 0.003,

  // Typography controls for the text mask.
  textSize: 25,
  lineHeight: 30
};